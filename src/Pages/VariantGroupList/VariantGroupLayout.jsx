import React from "react";
import { Outlet } from "react-router-dom";

const VariantGroupLayout = () => {
  return (
    <div>
      <Outlet></Outlet>
    </div>
  );
};

export default VariantGroupLayout;
