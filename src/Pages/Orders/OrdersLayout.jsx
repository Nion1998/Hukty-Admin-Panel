import React from "react";
import { Outlet } from "react-router-dom";

const OrdersLayout = () => {
  return (
    <div>
      <Outlet></Outlet>
    </div>
  );
};

export default OrdersLayout;
<Outlet></Outlet>;
